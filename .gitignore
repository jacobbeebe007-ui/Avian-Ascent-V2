node_modules/
dist/
.vite/

# The classic shell bundle is generated by scripts/build-bundle.js, but it MUST
# stay tracked: GitHub Pages (avianascent.com) deploys the repo root with no
# build step, and index.html loads ./js/avian-game.bundle.js. After editing js/
# or cached shell files, run `node scripts/build-bundle.js` and commit the
# bundle, hash, and sw.js together.
